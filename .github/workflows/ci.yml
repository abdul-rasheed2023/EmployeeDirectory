name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
          cache: true

      - name: Restore
        run: dotnet restore --locked-mode

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Test with coverage
        run: |
          dotnet test \
            --no-build \
            --configuration Release \
            --collect:"XPlat Code Coverage" \
            --results-directory ./coverage

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/

      - name: Verify Code Formatting
        run: dotnet format EmployeeDirectory.slnx --verify-no-changes --verbosity normal

      - name: Build Docker image
        run: docker build -t employee-directory:${{ github.sha }} .

      - name: Scan image for vulnerabilities
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: 'employee-directory:${{ github.sha }}'
          format: 'table'
          severity: 'CRITICAL'
          exit-code: '1'name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
          cache: true

      - name: Restore
        run: dotnet restore --locked-mode

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Test with coverage
        run: |
          dotnet test \
            --no-build \
            --configuration Release \
            --collect:"XPlat Code Coverage" \
            --results-directory ./coverage

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/

      - name: Verify Code Formatting
        run: dotnet format EmployeeDirectory.slnx --verify-no-changes --verbosity normal

      - name: Build Docker image
        run: docker build -t employee-directory:${{ github.sha }} .

      - name: Scan image for vulnerabilities
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: 'employee-directory:${{ github.sha }}'
          format: 'table'
          severity: 'CRITICAL'
          exit-code: '1'

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - name: Deploy placeholder
        run: echo "Deploying image employee-directory:${{ github.sha }} — no real target yet, this is the CD skeleton gate"
